{
  "opencollection": "1.0.0",
  "info": {
    "name": "Prometheus Alertmanager Admin API",
    "version": "v0.28.0"
  },
  "items": [
    {
      "info": {
        "name": "Admin",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Prometheus Create TSDB snapshot",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "http://{host}:{port}/api/v1/admin/tsdb/snapshot",
            "params": [
              {
                "name": "skip_head",
                "value": "",
                "type": "query",
                "description": "Skip data present in the head block if true. Defaults to false."
              }
            ]
          },
          "docs": "Creates a snapshot of all current data into the snapshots/ directory under the data directory and returns the directory name. Requires --web.enable-admin-api flag."
        },
        {
          "info": {
            "name": "Prometheus Delete series",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "http://{host}:{port}/api/v1/admin/tsdb/delete_series",
            "params": [
              {
                "name": "match[]",
                "value": "",
                "type": "query",
                "description": "Repeated label matcher argument to select series to delete."
              },
              {
                "name": "start",
                "value": "",
                "type": "query",
                "description": "Start of the time range to delete. Defaults to minimum time."
              },
              {
                "name": "end",
                "value": "",
                "type": "query",
                "description": "End of the time range to delete. Defaults to maximum time."
              }
            ]
          },
          "docs": "Deletes data for a selection of series in a time range by creating tombstones. The series data is not immediately removed but marked for deletion during the next compaction cycle. Requires --web.enable-admin-api flag."
        },
        {
          "info": {
            "name": "Prometheus Clean tombstones",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "http://{host}:{port}/api/v1/admin/tsdb/clean_tombstones"
          },
          "docs": "Removes the deleted data from disk and cleans up existing tombstones. This operation triggers a compaction on all blocks with tombstones. Requires --web.enable-admin-api flag."
        }
      ]
    }
  ],
  "bundled": true
}